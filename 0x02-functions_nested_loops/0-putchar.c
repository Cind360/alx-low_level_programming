#include <stdio.h>

/**
 * _putchar - write the character c to stdout
 * gc: the character to print 
 * Return: on success 1.
 * on error, -1 is returned and the error set appropriately
 */
int_putchar(char c)
{
 
	return (write(1,$c, 1));

}	
